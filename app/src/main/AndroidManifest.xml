<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.uta.utarepairingservices">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".CustomerHomeActivity" />
        <activity android:name=".AdminHomeActivity" />
        <activity android:name=".ServiceProviderHomeActivity" />
        <activity android:name=".ServiceCategoryActivity" />
        <activity android:name=".ViewProfileActivity" />
        <activity android:name=".SelectServiceProviderActivity" />
        <activity android:name=".BookAppointmentActivity" />
        <activity android:name=".ViewAppointmentStatusActivity" />
        <activity android:name=".ProvideRatingActivity"></activity>
    </application>

</manifest>